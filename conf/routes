# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /api/                   com.xebia.swfinance.controllers.Application.index

GET         /api/overview           com.xebia.swfinance.controllers.Application.overview
POST        /api/income             com.xebia.swfinance.controllers.Application.income
POST        /api/expenditure        com.xebia.swfinance.controllers.Application.expenditure

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file           controllers.Assets.at(path="/public", file)

    